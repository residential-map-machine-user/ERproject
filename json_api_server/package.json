{
  "name": "json_api_server",
  "version": "1.0.0",
  "description": "json api server mock作成",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch ./db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.12.0"
  }
}
